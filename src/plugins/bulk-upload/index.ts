import { PermissionDefinition } from "@etech/core";

export const createBulkUploadPermissionDefinition = new PermissionDefinition({
    name: 'CreateBulkUpload',
    description: 'Allows reading Charts on admin dashboard'
  });